import React from 'react'

export type UseUserData = {
    id: number;
    first_name: string;
    last_name: string;
    email: string;
    contact_number: string;
    address: string;
    created_at: string;
    updated_at: string;
  };
const UserData = () => {
    return
}

export default UserData
